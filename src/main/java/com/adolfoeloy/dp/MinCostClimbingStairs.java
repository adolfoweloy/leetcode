package com.adolfoeloy.dp;

/**
 * Given an array that represents steps in a staircase where each step ith represents the cost of that step,
 * find the minimum cost to reach the top-floor (the top-floor is not part of the staircase).
 * You can start from step 0 or 1, and you can jump 1 or 2 steps at once.
 */
public class MinCostClimbingStairs {

    public int minCostClimbingStairs(int[] cost) {
        return 0;
    }


}
